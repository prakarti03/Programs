#include<stdio.h>
#define PRAKU(a) ({\
printf("%d\n",a*a);\
printf("dev is a good boy");\
printf("\ndev is fatty boy small kid of paks");\
})
int main()
{
   // puts(PRAKU(6));
  // printf("%d",PRAKU(5)); printf returns the length of the string
   PRAKU(5);
    return 0;
}

